"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";

export default function NewProject(){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setError("");const form=new FormData(e.currentTarget);const keywords=String(form.get("keywords")||"").split(/[\n,]+/).map(x=>x.trim()).filter(Boolean);const res=await fetch("/api/projects",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),country:form.get("country"),language:form.get("language"),keywords})});const data=await res.json();setLoading(false);if(!res.ok)return setError(data.error);router.push("/dashboard");router.refresh();}return <main className="mx-auto min-h-screen max-w-3xl px-6 py-12"><button onClick={()=>router.back()} className="text-sm font-semibold text-slate-500">← Back</button><form onSubmit={submit} className="mt-6 rounded-3xl bg-white p-8 shadow-sm"><h1 className="text-3xl font-black">Create keyword project</h1><p className="mt-2 text-slate-500">Use one project per store, market or campaign.</p>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-red-700">{error}</p>}<label className="mt-6 block text-sm font-bold">Project / store name</label><input name="name" required className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"/><div className="mt-4 grid gap-4 sm:grid-cols-2"><div><label className="block text-sm font-bold">Country</label><input name="country" defaultValue="United Kingdom" required className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"/></div><div><label className="block text-sm font-bold">Language</label><input name="language" defaultValue="English" required className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"/></div></div><label className="mt-4 block text-sm font-bold">Keywords</label><textarea name="keywords" rows="12" placeholder="nike discount code\nnike promo code\nnike voucher code" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"/><p className="mt-2 text-xs text-slate-500">Enter one per line or separate with commas. Duplicates are removed automatically.</p><button disabled={loading} className="mt-6 rounded-xl bg-indigo-600 px-6 py-3 font-bold text-white disabled:opacity-60">{loading?"Creating...":"Create project"}</button></form></main>}
