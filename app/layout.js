import "./globals.css";

export const metadata = { title: "Keyword Traffic", description: "Bulk keyword traffic research platform" };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
